# Core Flask
flask>=2.3.0
flask-compress>=1.14
flask-login>=0.6.2
flask-sqlalchemy>=3.0.0
flask-bcrypt>=1.0.1
flask-limiter>=3.3.0
flask-cors>=4.0.0
werkzeug>=2.3.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# AI & ML — Gemini primary, Groq fallback
google-genai>=1.0.0
groq>=0.4.0

# YouTube transcript import (turn a video link into study material)
youtube-transcript-api>=0.6.2,<1.0

# PDF/Document Processing
# pypdf only. PyPDF2 was the old name, stopped at 3.0.1, and
# carries PYSEC-2026-1835 — pip-audit fails the build on it.
pypdf>=3.9.0
python-docx>=1.1.0

# Google Calendar Integration
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.100.0

# Notion Integration
notion-client>=2.2.1

# Web Push Notifications
pywebpush>=1.14.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
sentry-sdk>=1.32.0
pyjwt>=2.8.0
itsdangerous>=2.1.0
gunicorn>=21.0.0

flask-session>=0.8.0

# Payments
stripe>=8.0.0

# Analytics
posthog>=3.0.0
# Encryption at rest for third-party OAuth tokens (secret_box.py).
cryptography>=42.0.0
