"""Growth and revenue: retention measurement, plans, referral rewards.

Pure rules live here; ``growth_glue.py`` at the repo root owns the database
reads and the routes, matching the other ``*_glue.py`` modules.
"""
